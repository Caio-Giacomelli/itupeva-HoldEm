public class Deck 
{
    /*public Card[][] deck;
    public int NUM_SUITS = Suit.values().length;
    public int NUM_VALUES = Value.values().length;

    public Deck (){
        deck = new Card[NUM_SUITS][NUM_VALUES];
        Suit[] suits = Suit.values();
        Value[] values = Value.values();
        
        for (int suitIndex = 0; suitIndex < NUM_SUITS; suitIndex++) {
            Suit suit = suits[suitIndex];
            for (int valueIndex = 0; valueIndex < NUM_VALUES; valueIndex++) {
                Value value = values[valueIndex];
                deck[suitIndex][valueIndex] = new Card(suit, value);
            }
        }

    }*/
}

// Deck Implementation